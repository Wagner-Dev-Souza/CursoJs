// Não podemos criar constantes com palavras reservadas. (ex: let/ var / log / if etc... )
// Não podemos modificar constantes.
// Constantes precisam ter nomes significativos.
// Não podemos começar o nome de uma constante com números (ex: 1nome, 32cliente).
// Não podem conter espaços ou traços. (ex: nome do cliente / nome-cliente)
// Utilizamos por convenção camelCase. (ex: nome, nomeCompleto, nomeDoCliente)
// Case-sensitive
// NÃO UTILIZA VAR, UTILIZE CONST.

const nome = 'João';
console.log(nome);

//operadores matemáticos (+ - * /)
const primeiroNumero = 5;
const segundoNumero = 10;
const resultado = primeiroNumero * segundoNumero;
console.log (resultado);
const resultadoDuplicado = resultado * 2;
console.log (resultadoDuplicado);
let resultadoTriplicado = resultado * 3;
console.log (resultadoTriplicado);